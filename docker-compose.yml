version: '3'

services:
  app:
    build: .
    ports:
      - "8443:443"
    volumes:
      - "./data/config:/app/config"
      - "./data/letsencrypt:/etc/letsencrypt"
    environment:
      DOMAIN: ""
      MAIL: ""
